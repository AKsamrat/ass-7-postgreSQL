## <code style="color:green"> Assignment 7 (Postgress)</code>
### <code style="color:aqua">What is PostgreSQL</code>
-PostgreSQL হল একটি ওপেন-সোর্স, শক্তিশালী  রিলেশনাল ডাটাবেস ম্যানেজমেন্ট সিস্টেম  যা SQL  ব্যবহার করে ডাটাবেস পরিচালনা করে। এটি Scalability, Security, এবং Extensibility ।

### <code style="color:aqua">What is the purpose of a database schema in PostgreSQ</code>
- স্কিমা হল ডাটাবেস অবজেক্ট গুছিয়ে রাখার একটি পদ্ধতি
PostgreSQL-এ স্কিমার প্রধান কাজগুলো:
1️⃣ ডাটাবেস অবজেক্ট গুছিয়ে রাখা:

স্কিমা ব্যবহার করলে একই ডাটাবেসের মধ্যে বিভিন্ন টেবিল ও ভিউ শ্রেণীবদ্ধভাবে রাখা যায়, যা ম্যানেজ করা সহজ করে।

2️⃣ নিরাপত্তা ও অ্যাক্সেস কন্ট্রোল বৃদ্ধি করা :

PostgreSQL-এ বিভিন্ন স্কিমার জন্য আলাদা অনুমতি (Permission) সেট করা যায়, যা ডাটা নিরাপদ রাখে।

যেমন: dev_schema ডেভেলপারদের জন্য এবং admin_schema অ্যাডমিনদের জন্য আলাদা রাখা যায়।

3️⃣ নাম দ্বন্দ্ব (Name Conflict) এড়ানো :

স্কিমা ব্যবহার করে একই নামের একাধিক টেবিল রাখা সম্ভব।

যেমন: sales.orders এবং inventory.orders একই ডাটাবেসে থাকতে পারে।

4️⃣ মাল্টি-টেন্যান্ট অ্যাপ্লিকেশনের জন্য কার্যকরী :

যদি একটি সফটওয়্যার বিভিন্ন গ্রাহকের জন্য পৃথক ডাটা রাখে, তাহলে প্রত্যেক গ্রাহকের জন্য আলাদা স্কিমা ব্যবহার করা যায়।
### <code style="color:aqua">Explain the Primary Key and Foreign Key concepts in PostgreSQL</code>

- Primary Key :
Primary Key হলো একটি ইউনিক কলাম বা কলামের সমষ্টি, যা প্রতিটি রেকর্ডকে অন্য রেকর্ড থেকে আলাদা করে।

📌 Primary Key-এর বৈশিষ্ট্য <br>
✔️ ইউনিক (Unique) হতে হবে → একই টেবিলে একাধিক রেকর্ডের জন্য একই Primary Key মান থাকতে পারবে না।<br>
✔️ NULL হতে পারবে না → অর্থাৎ Primary Key ফিল্ডে অবশ্যই একটি মান থাকতে হবে।<br>
✔️ একটি টেবিলে শুধুমাত্র একটি Primary Key থাকতে পারে।<br>

- Foreign Key :<br>
Foreign Key হল একটি কলাম বা কলামের সেট, যা অন্য টেবিলের Primary Key কে রেফারেন্স (Reference) করে। এটি দুটি টেবিলের মধ্যে সম্পর্ক (Relationship) স্থাপন করে।<br>

📌 Foreign Key-এর বৈশিষ্ট্য<br>
✔️ অন্য একটি টেবিলের Primary Key-এর উপর নির্ভরশীল।<br>
✔️ ডাটা ইন্টিগ্রিটি বজায় রাখে → অর্থাৎ ভুল সম্পর্কযুক্ত ডাটা ঢুকতে দেয় না।<br>
✔️ ক্যাসকেড অপশন (CASCADE) ব্যবহার করে সম্পর্কিত ডাটা মুছে ফেলা বা আপডেট করা যায়।<br>


### <code style="color:aqua">What is the difference between the VARCHAR and CHAR data types?</code>

- VARCHAR এবং CHAR উভয়ই স্ট্রিং ডাটা টাইপ, যা টেক্সট ডাটা সংরক্ষণের জন্য ব্যবহৃত হয়। তবে, এদের মধ্যে কিছু গুরুত্বপূর্ণ পার্থক্য রয়েছে।

CHAR(n) (Fixed Length):-
এটি একটি নির্দিষ্ট দৈর্ঘ্যের স্ট্রিং সংরক্ষণ করে।

যদি কোনো স্ট্রিং ছোট হয়, তাহলে অবশিষ্ট জায়গা ফাঁকা (padded with spaces) দিয়ে পূরণ করা হয়।

পারফরম্যান্স: CHAR ফিক্সড সাইজ হওয়ায় স্টোরেজ অপ্টিমাইজড, তবে অপ্রয়োজনীয় স্পেস সংরক্ষণ করে।

 VARCHAR(n) (Variable Length):-
এটি পরিবর্তনশীল দৈর্ঘ্যের স্ট্রিং সংরক্ষণ করে (অর্থাৎ, শুধুমাত্র প্রয়োজনীয় জায়গা নেয়)।

যদি স্ট্রিং ছোট হয়, তবে অতিরিক্ত স্পেস সংরক্ষণ করা হয় না।

পারফরম্যান্স: সাধারণত বেশি দক্ষ ও স্টোরেজ বান্ধব, কারণ এটি অপ্রয়োজনীয় স্পেস সংরক্ষণ করে না।

### <code style="color:aqua">Explain the purpose of the WHERE clause in a SELECT statement</code>
- 🔹 WHERE ক্লজের উদ্দেশ্য :-
WHERE ক্লজ PostgreSQL-এ একটি ফিল্টারিং টুল, যা নির্দিষ্ট শর্ত অনুযায়ী ডাটা নির্বাচন করতে ব্যবহার করা হয়। এটি ডাটাবেস থেকে সুনির্দিষ্ট রেকর্ড ফেরত দিতে সাহায্য করে, যা কোয়েরির কার্যকারিতা এবং পারফরম্যান্স উন্নত করে।

✅ WHERE ক্লজ কেন ব্যবহার করা হয়?:-<br>
1️⃣ নির্দিষ্ট ডাটা নির্বাচন করতে → শুধুমাত্র প্রয়োজনীয় রেকর্ড বের করে।<br>
2️⃣ ডাটাবেস পারফরম্যান্স বাড়াতে → অপ্রয়োজনীয় ডাটা বাদ দিয়ে দ্রুত ফলাফল দেয়।<br>
3️⃣ ফিল্টারিং ও শর্ত যোগ করতে → নির্দিষ্ট ক্রাইটেরিয়ার ভিত্তিতে ডাটা ফিল্টার করা যায়<br>

### <code style="color:aqua">What are the LIMIT and OFFSET clauses used for?</code>
-  1️⃣ LIMIT ক্লজ :-
LIMIT ব্যবহার করে ফলাফলের সংখ্যার সীমা নির্ধারণ করা যায়, যাতে কেবল নির্দিষ্ট সংখ্যক রেকর্ড ফেরত আসে।

🔹 LIMIT সিনট্যাক্স:

SELECT column_name 
FROM table_name 
LIMIT number;

- 2️⃣ OFFSET ক্লজ
OFFSET ক্লজ ব্যবহার করে প্রথম কিছু সংখ্যক রেকর্ড বাদ দিয়ে পরবর্তী রেকর্ডগুলো রিটার্ন করা যায়।

🔹 OFFSET সিনট্যাক্স:

SELECT column_name 
FROM table_name 
OFFSET number;

### <code style="color:aqua">How can you modify data using UPDATE statements?</code>

-নির্দিষ্ট রেকর্ড আপডেট করা
📌 উদাহরণ: একজন কর্মীর বেতন পরিবর্তন করা

UPDATE employees 
SET salary = 60000 
WHERE id = 3;

### <code style="color:aqua">What is the significance of the JOIN operation, and how does it work in PostgreSQL?</code>

- PostgreSQL এ JOIN অপারেশনের গুরুত্ব এবং এটি কিভাবে কাজ করে তা বাংলায় নিচে ব্যাখ্যা করা হলো:

JOIN অপারেশনের গুরুত্ব:

ডাটাবেসে, ডেটা সাধারণত একাধিক টেবিলে বিভক্ত থাকে। এই টেবিলগুলোর মধ্যে সম্পর্ক স্থাপন করে প্রয়োজনীয় ডেটা পুনরুদ্ধার করার জন্য JOIN অপারেশন অত্যন্ত গুরুত্বপূর্ণ। এর মাধ্যমে:

সম্পর্কিত ডেটা একত্রীকরণ: একাধিক টেবিল থেকে সম্পর্কযুক্ত ডেটা একত্রিত করে একটি একক ফলাফল সেট তৈরি করা যায়।
ডেটা বিশ্লেষণ: জটিল প্রশ্ন এবং রিপোর্টের জন্য বিভিন্ন টেবিলের ডেটা বিশ্লেষণ করা সহজ হয়।
ডেটা সামঞ্জস্য রক্ষা: একাধিক টেবিলের মধ্যে সম্পর্ক বজায় রেখে ডেটার সামঞ্জস্য রক্ষা করা যায়।
তথ্য পুনরুদ্ধার: একাধিক টেবিল থেকে প্রয়োজনীয় তথ্য পুনরুদ্ধার করা যায় যা একটি টেবিল থেকে সম্ভব নয়।
সহজভাবে বলতে গেলে, JOIN অপারেশন বিভিন্ন টেবিলের মধ্যে সম্পর্ক তৈরি করে ডেটা একত্রিত করতে সাহায্য করে।

PostgreSQL এ JOIN কিভাবে কাজ করে:

PostgreSQL এ বিভিন্ন ধরণের JOIN অপারেশন রয়েছে, যার প্রত্যেকটির নিজস্ব কাজ এবং বৈশিষ্ট্য রয়েছে। নিচে কয়েকটি গুরুত্বপূর্ণ JOIN অপারেশন ব্যাখ্যা করা হলো:

INNER JOIN:

এই JOIN অপারেশন দুটি টেবিলের মধ্যে শুধুমাত্র সেই সারিগুলো ফেরত দেয় যেখানে JOIN শর্তটি পূরণ হয়।
অর্থাৎ, উভয় টেবিলেই মিল থাকা সারিগুলোই ফলাফল হিসেবে পাওয়া যায়।
উদাহরণ:

SELECT orders.order_id, customers.customer_name
FROM orders
INNER JOIN customers ON orders.customer_id = customers.customer_id;
এই উদাহরণে, orders এবং customers টেবিলের মধ্যে customer_id এর ভিত্তিতে মিল থাকা সারিগুলো ফেরত দেওয়া হবে।
LEFT JOIN (or LEFT OUTER JOIN):

এই JOIN অপারেশন বাম টেবিলের (left table) সমস্ত সারি এবং ডান টেবিলের (right table) মিল থাকা সারিগুলো ফেরত দেয়।
ডান টেবিলে যদি মিল না থাকে, তবে ডান টেবিলের কলামগুলোর জন্য NULL মান প্রদর্শিত হবে।
উদাহরণ:


SELECT customers.customer_name, orders.order_id
FROM customers
LEFT JOIN orders ON customers.customer_id = orders.customer_id;
এই উদাহরণে, customers টেবিলের সব সারি এবং orders টেবিলে মিল থাকা সারিগুলো ফেরত দেওয়া হবে। যদি কোনো গ্রাহকের অর্ডার না থাকে, তবে order_id এর জন্য NULL মান প্রদর্শিত হবে।
RIGHT JOIN (or RIGHT OUTER JOIN):

এই JOIN অপারেশন ডান টেবিলের (right table) সমস্ত সারি এবং বাম টেবিলের (left table) মিল থাকা সারিগুলো ফেরত দেয়।
বাম টেবিলে যদি মিল না থাকে, তবে বাম টেবিলের কলামগুলোর জন্য NULL মান প্রদর্শিত হবে।
উদাহরণ:
        SELECT customers.customer_name, orders.order_id
FROM customers
RIGHT JOIN orders ON customers.customer_id = orders.customer_id;
```   

FULL JOIN (or FULL OUTER JOIN):

এই JOIN অপারেশন উভয় টেবিলের সমস্ত সারি ফেরত দেয়।
যদি কোনো টেবিলে মিল না থাকে, তবে অন্য টেবিলের কলামগুলোর জন্য NULL মান প্রদর্শিত হবে।
উদাহরণ:


SELECT customers.customer_name, orders.order_id
FROM customers
FULL JOIN orders ON customers.customer_id = orders.customer_id;
CROSS JOIN:   

এই JOIN অপারেশন দুটি টেবিলের প্রতিটি সারির সাথে অন্য টেবিলের প্রতিটি সারি মিলিয়ে দেয়।
এটি কার্টেসিয়ান প্রোডাক্ট তৈরি করে।
উদাহরণ:


SELECT customers.customer_name, products.product_name
FROM customers
CROSS JOIN products 

### <code style="color:aqua">Explain the GROUP BY clause and its role in aggregation operations?</code>

- GROUP BY ক্লজ SQL-এর একটি গুরুত্বপূর্ণ অংশ, যা একটি টেবিলের সারিগুলোকে এক বা একাধিক কলামের মানের ভিত্তিতে গ্রুপ করতে ব্যবহৃত হয়। এটি মূলত ডেটা বিশ্লেষণ এবং রিপোর্টিংয়ের জন্য ব্যবহৃত হয়।

সহজভাবে বলতে গেলে, GROUP BY ক্লজ আপনাকে একই মানের সারিগুলোকে একত্রিত করতে এবং তাদের উপর অ্যাগ্রিগেশন ফাংশন প্রয়োগ করতে সাহায্য করে।

অ্যাগ্রিগেশন অপারেশনে GROUP BY-এর ভূমিকা:

অ্যাগ্রিগেশন ফাংশনগুলো (যেমন COUNT, SUM, AVG, MIN, MAX) ডেটার একটি সেটের উপর কাজ করে এবং একটি একক মান ফেরত দেয়। GROUP BY ক্লজ ব্যবহার করে, আপনি এই ফাংশনগুলো প্রতিটি গ্রুপের উপর প্রয়োগ করতে পারেন এবং প্রতিটি গ্রুপের জন্য একটি পৃথক ফলাফল পেতে পারেন।

এখানে GROUP BY ক্লজের কিছু গুরুত্বপূর্ণ ভূমিকা উল্লেখ করা হলো:

ডেটা গ্রুপ করা:

GROUP BY ক্লজ একটি বা একাধিক কলামের মানের উপর ভিত্তি করে সারিগুলোকে গ্রুপ করে।
উদাহরণস্বরূপ, আপনি "শহর" কলামের ভিত্তিতে গ্রাহকদের গ্রুপ করতে পারেন।
অ্যাগ্রিগেশন ফাংশন প্রয়োগ করা:

GROUP BY ক্লজ ব্যবহার করে, আপনি প্রতিটি গ্রুপের উপর অ্যাগ্রিগেশন ফাংশন প্রয়োগ করতে পারেন।
উদাহরণস্বরূপ, আপনি প্রতিটি শহরের গ্রাহকের সংখ্যা গণনা করতে পারেন।
রিপোর্টিং এবং বিশ্লেষণ:

GROUP BY ক্লজ ডেটা বিশ্লেষণ এবং রিপোর্টিংয়ের জন্য অত্যন্ত কার্যকর।
এটি আপনাকে ডেটার সারসংক্ষেপ তৈরি করতে এবং বিভিন্ন গ্রুপের মধ্যে তুলনা করতে সাহায্য করে।
ডেটা সামারাইজ করা:

অনেকগুলো রো থেকে ডেটা নিয়ে অল্প কিছু রো এর মধ্যে সামারাইজ করার জন্য গ্রুপ বাই ব্যবহার করা হয়।
যেমন, কোন শহরে কতজন কাস্টমার আছে, এই তথ্য বের করার জন্য।
উদাহরণ:

ধরুন, আমাদের একটি "orders" টেবিল আছে, যেখানে অর্ডার সম্পর্কিত তথ্য রয়েছে। আমরা প্রতিটি গ্রাহকের মোট অর্ডারের সংখ্যা জানতে চাই। এর জন্য আমরা নিম্নলিখিত SQL ক্যোয়ারী ব্যবহার করতে পারি:

SELECT customer_id, COUNT(*) AS total_orders
FROM orders
GROUP BY customer_id;
এই ক্যোয়ারীটি "customer_id" কলামের ভিত্তিতে সারিগুলোকে গ্রুপ করে এবং প্রতিটি গ্রাহকের অর্ডারের সংখ্যা গণনা করে। ফলাফলটি "customer_id" এবং "total_orders" কলামের একটি টেবিল হিসাবে প্রদর্শিত হবে।

আরো একটি উদাহরণ:

যদি আমরা জানতে চাই প্রতিটি শহরে কতজন কাস্টমার আছে, তাহলে আমরা নিচের কোয়েরিটি ব্যবহার করতে পারি।



SELECT city, COUNT(customer_id) as customer_count
FROM customers
GROUP BY city;
এখানে, city কলামের ভিত্তিতে ডেটা গ্রুপ করা হয়েছে এবং COUNT(customer_id) ফাংশন ব্যবহার করে প্রতিটি শহরের কাস্টমারের সংখ্যা বের করা হয়েছে।


### <code style="color:aqua">How can you calculate aggregate functions like COUNT(), SUM(), and AVG() in PostgreSQL?</code>


- অ্যাগ্রিগেট ফাংশনগুলি ডেটার একটি সেটের উপর কাজ করে এবং একটি একক মান ফেরত দেয়। এগুলি সাধারণত ডেটা বিশ্লেষণ এবং রিপোর্টিংয়ের জন্য ব্যবহৃত হয়। PostgreSQL এ কিছু গুরুত্বপূর্ণ অ্যাগ্রিগেট ফাংশন হলো:

COUNT(): সারি বা মানের সংখ্যা গণনা করে।
SUM(): একটি কলামের সমস্ত মানের যোগফল গণনা করে।
AVG(): একটি কলামের সমস্ত মানের গড় গণনা করে।
MIN(): একটি কলামের সর্বনিম্ন মান খুঁজে বের করে।
MAX(): একটি কলামের সর্বোচ্চ মান খুঁজে বের করে।


COUNT() ফাংশন:

COUNT() ফাংশন একটি টেবিলের সারি বা একটি কলামের নন-NULL মানের সংখ্যা গণনা করে।

উদাহরণ:

SQL

SELECT COUNT(*) FROM customers; -- টেবিলের মোট সারি সংখ্যা গণনা করে
SELECT COUNT(email) FROM customers; -- email কলামের নন-NULL মানের সংখ্যা গণনা করে
COUNT(DISTINCT column_name) ব্যবহার করে ইউনিক মানের সংখ্যা বের করা যায়।

SELECT COUNT(DISTINCT city) FROM customers;
SUM() ফাংশন:

SUM() ফাংশন একটি সংখ্যাসূচক কলামের সমস্ত মানের যোগফল গণনা করে।

উদাহরণ:
SELECT SUM(order_total) FROM orders; -- order_total কলামের সমস্ত মানের যোগফল গণনা করে
AVG() ফাংশন:

AVG() ফাংশন একটি সংখ্যাসূচক কলামের সমস্ত মানের গড় গণনা করে।

উদাহরণ:

SELECT AVG(order_total) FROM orders; -- order_total কলামের সমস্ত মানের গড় গণনা করে
GROUP BY ক্লজের সাথে অ্যাগ্রিগেট ফাংশন ব্যবহার:

অ্যাগ্রিগেট ফাংশনগুলি সাধারণত GROUP BY ক্লজের সাথে ব্যবহৃত হয় যাতে প্রতিটি গ্রুপের জন্য অ্যাগ্রিগেট মান গণনা করা যায়।

উদাহরণ:
SELECT customer_id, COUNT(order_id) AS total_orders
FROM orders
GROUP BY customer_id;
এই ক্যোয়ারীটি প্রতিটি customer_id এর জন্য অর্ডারের সংখ্যা গণনা করে।

আরো একটি উদাহরণ:

SELECT city, AVG(salary) as average_salary
FROM employees
GROUP BY city;
এই কোয়েরিটি প্রতিটি শহরের গড় বেতন বের করে।

WHERE ক্লজের সাথে অ্যাগ্রিগেট ফাংশন ব্যবহার:

অ্যাগ্রিগেট ফাংশনগুলো WHERE ক্লজের সাথে ব্যবহার করে, নির্দিষ্ট শর্তের উপর ভিত্তি করে ডেটা ফিল্টার করে অ্যাগ্রিগেট মান গণনা করা সম্ভব।

উদাহরণ:

SELECT COUNT(*) FROM orders WHERE order_date > '2023-01-01';